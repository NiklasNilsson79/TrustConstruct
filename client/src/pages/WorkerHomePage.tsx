import { LogoutButton } from '../auth/LogoutButton';

export function WorkerHomePage() {
  return (
    <main>
      <h1>Worker</h1>
      <p>Placeholder page for worker dashboard.</p>
      <LogoutButton />
    </main>
  );
}
